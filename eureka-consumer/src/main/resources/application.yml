server:
  port: 80
spring:
  application:
    name: eureka-consumer
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:1001/eureka